import { QInsertDirective } from './q-insert.directive';

describe('QInsertDirective', () => {
  it('should create an instance', () => {
    const directive = new QInsertDirective();
    expect(directive).toBeTruthy();
  });
});
